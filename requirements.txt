fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
yt-dlp>=2025.1.1
opencv-python-headless>=4.10
python-dotenv>=1.0
markdown>=3.5
python-multipart>=0.0.9

# 本地语音识别兜底（无可用字幕时；模型文件需自备到 models/，见 README）
faster-whisper>=1.0
# Windows 下 ctranslate2 的 CUDA 运行库（GPU 加速用，CPU-only 可不装）
nvidia-cublas-cu12
nvidia-cudnn-cu12
